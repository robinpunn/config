#!/bin/sh

case $BLOCK_BUTTON in
    1) toggle-bluetooth >/dev/null 2>&1 & ;;
    3) st -e sh -c "bluetoothctl devices; bluetoothctl" ;; 
esac

if ! bluetoothctl show | grep -q "Powered: yes"; then
	icon="󰂲"
elif bluetoothctl devices Connected | grep -q .; then
	icon="󰂱"
else
	icon=""
fi

printf "%s " "$icon"
